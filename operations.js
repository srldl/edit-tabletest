function add(x,y)  {
  return x + y;
}

function subtract(x,y) {
  return x - y;
}

function multiply(x,y) {
  return x * y;
}

export default {
   add: add,
   subtract: subtract,
   multiply: multiply
}
